SPORTOTEKA Tracker — Team Live v58

Исправлено по логам 24.07.2026:

1. BLE-пул теперь общий для всех открытых экранов одной команды.
   Повторно открытый Live больше не теряет уже подключённые GPS-каналы.
2. Live-панель перестраивается сразу после подключения/отключения GPS.
3. Игроки с реально подключёнными и назначенными GPS автоматически
   появляются в верхней ленте и в сетке Live без ручного выбора.
4. Сопоставление учитывает players.id/user_id и MAC/iOS UUID-алиасы.
5. Ответ пакетного старта связывается с каналом по UUID, имени физического
   $ATP-трекера или player_id.
6. Live не показывает ложный успешный старт, если сервер не вернул session_id.
7. Добавлены debug-события workspace_team_live_preflight,
   workspace_team_live_start_error и team_live_ui_start_error.
8. Исправлено горизонтальное RenderFlex overflow в строке управления Live.

Ожидаемый сценарий:

- $ATP-C1580 → Ярослав Скидан;
- $ATP-C1685 → Кирилл Дегтерев;
- после открытия Live сверху сразу видны оба игрока;
- кнопка «Старт Live» создаёт две отдельные серверные Live-сессии.

Установка:
распаковать архив в корень Flutter-проекта с заменой
lib/presentation/tracker.

PHP и SQL для v58 менять не требуется.
